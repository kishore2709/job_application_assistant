# Fallback Anthropic key used only if no key is set in Settings -> AI Provider.
# AI provider, model selection, and other provider keys (OpenAI/Google) are
# now configured in the app's Settings tab, not via .env.
ANTHROPIC_API_KEY=your-anthropic-api-key-here
RAPIDAPI_KEY=your-rapidapi-key-here
